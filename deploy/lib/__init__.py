"""Shared library for FastINK deployment tooling.

Provides CLI UI utilities, default configuration management,
filesystem helpers, host pre-checks, template rendering, and
path planning — all consumed by the ``cmd/`` command modules.
"""
